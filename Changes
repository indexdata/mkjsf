- Adds parameter 'windowid' (SP extension) to init command
- Fix for state handling in record command (the SP extensions) 
- Adds working web.xml context parameters for SP service

0.3.3  

- First release